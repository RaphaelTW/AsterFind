# AsterFind Relay

Relay mínimo do AsterFind baseado em Cloudflare Workers + D1.

Ele não conhece a chave de criptografia do payload. Armazena somente ciphertext e hashes de tokens.

Consulte o README da raiz para setup completo.
